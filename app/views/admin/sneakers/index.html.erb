<div class="container">
  <div class="row d-flex flex-column">
  <div class="row mx-auto">
    <h1>スニーカー一覧</h1>
  </div>
  <% @sneakers.each do |sneaker| %>
    <div class="row">
      <%= image_tag sneaker.get_image %>
      <p>スニーカー名:<%= link_to admin_sneaker_path(sneaker.id) do %><%= sneaker.sneaker_name_ja %><% end %></p>
    </div>
  <% end %>
  </div>
</div>