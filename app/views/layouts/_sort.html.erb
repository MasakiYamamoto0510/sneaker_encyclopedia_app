<%= form_with url: form_url, method: :get, local: true do %>
  <label for="sort">並び順：</label>
  <%= select_tag :sort, options_for_select([["[年代]新しい順", "latest"], ["[年代]古い順", "old"]], selected: params[:sort]) %>
  <%= submit_tag "並び替え" %>
<% end %>