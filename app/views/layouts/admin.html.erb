<!DOCTYPE html>
<html>
  <head>
    <title>Admin - Sneaker Encyclopedia App</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">

    <script src="https://kit.fontawesome.com/81447d50c8.js" crossorigin="anonymous"></script>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body class="d-flex flex-column vh-100">
    <header class="shadow-sm">
      <nav class="navbar navbar-expand-lg navbar-light">
        <div class="container">
        <a class="navbar-brand p-3" href="/">スニーカー図鑑</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="navbarNavDropDown" 
                         aria-controls="navbarNavDropDown" aria-expanded="false" aria-label="Toggle navigation">
          <sapn class="navbar-toggler-icon"></sapn>
        </button>
          <div class="collapse navbar-collapse" id="navbarNavDropDown">
            <ul class="navbar-nav ml-auto">
              <% if admin_signed_in? %>
                <li class="nav-item">
                 <%= link_to 'ログアウト', destroy_admin_session_path, method: :delete, class: 'nav-link text-dark' %>
                </li>
                <li class="nav-item">
                 <%= link_to 'ダッシュボード', admin_dashboards_path, class: 'nav-link text-dark' %>
                </li>
                <li class="nav-item">
                 <%= link_to 'カスタマー一覧', admin_customers_path, class: 'nav-link text-dark' %>
                </li>
                <li class="nav-item">
                 <%= link_to '新規発掘申請一覧', admin_unpublished_path, class: 'nav-link text-dark' %>
                </li>
                <li class="nav-item">
                 <%= link_to 'スニーカー一覧', admin_sneakers_path, class: 'nav-link text-dark' %>
                </li>
                
                <li class="nav-item">
                 <%= link_to 'スニーカー作成',  admin_dashboards_sneaker_path, class: 'nav-link text-dark' %>
                </li>
              <% end %>
            </ul>
          </div>
        </div>
      </nav>
    </header>
    <main class="container">
      <% flash.each do |message_type, message| %>
        <div class="alert alert-<%= message_type %>"><%= message %></div>
      <% end %>

      <div class="d-flex justify-content-center mb-2">
        <% if current_user_type.present? %>
          <%= render 'layouts/search', user_type: current_user_type %>
        <% end %>
      </div>
      
      <%= yield %>
    </main>
  </body>
</html>
